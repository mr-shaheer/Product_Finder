[project]
name = "product-finder"
version = "0.1.0"
description = "AI-powered product finder built with the OpenAI Agents SDK."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.141.1",
    "httpx>=0.28.1",
    "openai-agents>=0.18.0",
    "pydantic>=2.13.4",
]
